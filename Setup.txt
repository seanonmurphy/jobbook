Install python 3.4.2

Create a new virtual env (as we use python 3 to create the env, python will default to 3 whilst in the env):
>py -3 -m venv env

Start the virtual environment:
>env\Scripts\activate.bat

Install django 1.9:
>pip install django==1.9

Make database migrations and migrate:
>python manage.py makemigrations
>python manage.py migrate

Create admin superuser:
>python manage.py createsuperuser

Username: admin
password: f


Start Django project:
>django-admin startproject jobbook .

Create database (sqlite3):
>python manage.py migrate

Create application:
>python manage.py startapp core

GIT:
>git init
>git status
>git add -A .
>git commit -m "Initial commit."

>git remote add origin https://github.com/seanonmurphy/jobbook.git
>git push -u origin master
